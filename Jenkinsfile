#!/use/bin/groovy
// declarative
pipeline {
    agent any
    stages {
        stage('Build') {
            steps{
                echo "build..."
            }
        }

        stage('Test') {
            steps{
                echo "testing..."
            }
        }

        stage('Deploy') {
            steps{
                echo "deploy..."
            }
        }
    }
}